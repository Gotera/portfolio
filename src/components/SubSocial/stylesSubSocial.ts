import styled from 'styled-components'

export const SubLine = styled.span`
    width: 2px;
    height: 8rem;
    background-color: #1a41dc;
`
export const SubSocial = styled.div`
    display: flex;
    flex-direction: column;
    align-items: center;
    z-index: -3;

    position: fixed;
    bottom: 0;
    left: 2rem;

    a {
        text-decoration: none;
    }
    ul {
        list-style-type: none;
    }
`
